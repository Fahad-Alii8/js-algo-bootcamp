/**
 *
 * @param{number} a - The first number.
 * @param{number} b - The second number.
 * @returns{number} The result sum of two numbers.
 *
 */

function getSum(a, b) {
  return a + b;
}

module.exports = getSum;
